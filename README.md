# chat-cli

A lightweight terminal chat written in Python for AI interaction.

## How to Use

**Build and run with Docker:**
```bash
docker build -t chat-cli .
docker run --rm chat-cli
```

Or combined: `docker build -t chat-cli . && docker run --rm chat-cli`